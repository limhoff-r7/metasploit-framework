en:
  metasploit:
    model:
      errors:
        models:
          metasploit/framework/command/exploit/simple:
            attributes:
              rank_number:
                minimum_rank: "is (%{rank_number} aliased as %{rank_name}) less than the minimum rank number (%{minimum_rank_number} aliased as %{minimum_rank_name})"
          metasploit/framework/command/use/set_metasploit_instance:
            attributes:
              words:
                wrong_length: "is the wrong length (should be %{count} words)"
          metasploit/framework/database_connection:
            attributes:
              connected?:
                inclusion: "is false"
              database_yaml_pathname:
                non_existent: "does not exist"
                unreadable: "is not readable"
          metasploit/framework/module/ancestor/metasploit_module:
            attributes:
              base:
                unusable: "is not usable from this host system"
  errors:
    messages:
      defanged: 'is defanged and cannot perform dangerous actions.'
      invalid_module_type: "is not a module type.  Valid module types are defined in `Metasploit::Model::Module::Type::ALL` (%{valid_module_types})."
      invalid_module_types: "is not a collection of module types.  Module types should be an `Array<String>`, which is a subset of `Metasploit::Model::Module::Type::ALL` (%{valid_module_types})."
      nil: "cannot be nil, but can be empty."
